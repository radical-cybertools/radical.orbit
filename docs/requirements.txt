# MkDocs toolchain, kept in sync with the sibling repos (rhapsody,
# radical.asyncflow).  The `<2` guard is deliberate: MkDocs 1.x is
# unmaintained and the announced "MkDocs 2.0" is backward-incompatible
# (see https://squidfunk.github.io/mkdocs-material/blog/2026/02/18/mkdocs-2.0/);
# without the pin a future `pip install` could silently break the RTD build.
mkdocs>=1.5.0,<2.0
mkdocs-material>=9.0.0
mkdocstrings[python]>=0.24.0
mkdocs-gen-files>=0.5.0
mkdocs-literate-nav>=0.6.0
mkdocs-section-index>=0.3.0
